import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    commentContainer: {
        padding: 20,
        borderBottomWidth: 1,
        borderBottomColor: '#ccc',
      },

      arrowBtn: {
        marginBottom: 10,
        marginTop: 20,
        marginLeft: 20
      }

});