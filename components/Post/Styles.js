import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    card:{
        marginTop: 50
    },

    upVote:{
        color: 'orange',
        fontSize: 20,
        fontWeight: 'bold',
        marginRight: 10,
        marginLeft: 10,
        marginTop: 10,
        marginBottom: 10,
        textAlign: 'center'
    }
});